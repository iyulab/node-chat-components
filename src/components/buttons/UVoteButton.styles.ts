import { css } from "lit";

export const styles = css`
  :host {
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  u-button {
    color: var(--u-txt-color-weak);
    font-size: 16px;
  }
`;
