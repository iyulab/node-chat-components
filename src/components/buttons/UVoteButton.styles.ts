import { css } from "lit";

export const styles = css`
  :host {
    display: inline-flex;
    flex-direction: row;
    align-items: center;
    gap: 4px;
  }

  u-button {
    color: var(--u-txt-color-weak);
    font-size: 16px;
  }
`;
