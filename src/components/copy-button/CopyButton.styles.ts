import { css } from "lit";

export const styles = css`
  :host {
    display: inline-flex;
  }

  uc-button {
    border: none;
  }

  uc-icon {
    font-size: 14px;
    color: var(--uc-text-color-medium);
  }
`;
